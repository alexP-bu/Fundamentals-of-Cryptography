public class StreamCipher {
    //Let G be a PRG defined over {0,1}^l -> {0,1}^L
    //Let Stream Cipher Esc = (E, D) constructed from G as follows:
    //E(s, m) := G(s)[0 . . v − 1] ⊕ m
    //D(s, c) := G(s)[0 . . v − 1] ⊕ c.
}
